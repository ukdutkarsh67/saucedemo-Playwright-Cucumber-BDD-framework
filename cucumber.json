{
    "default":{
        "formatOptions": {
            "snippetInterface": "async-await"
        },
        "paths": ["tests/features/*.feature"],
        
        "require": ["tests/steps/*.ts"],
        "requireModule": [
            "ts-node/register"
        ]
    }
}